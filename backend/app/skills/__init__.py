"""Guardian AI skills (deterministic tool functions).

Each module exposes pure-Python tools that critic & resolver agents call.
They are also wrapped as LangChain `@tool`s in `app.skills.tools` for binding.
"""
